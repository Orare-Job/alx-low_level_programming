#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 * @r: the integer whose absoute value is computed
 * Return: Absolute value
 */

int _abs(int r)
{
	if (r > 0)
	{
		return (r * 1);
	}
	else
	{
		return (r * -1);
	}
}
