#include "main.h"

/**
 * add - adds two integers and returns the result
 * @x: First integer
 * @y: Second integer
 * Return: Result of the added numbers (integer)
 */

int add(int x, int y)
{
	int result = x + y;

	return (result);
}
